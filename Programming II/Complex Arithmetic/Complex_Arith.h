//prototypes of the complex arithmetic

#include "complex.h"

#ifndef 	COMPLEX_ARITH
#define		COMPLEX_ARITH

complex Add_Complex(complex, complex);
complex Sub_Complex(complex, complex);
complex Mul_Complex(complex, complex);
complex Div_Complex(complex, complex);
complex Con_Complex(complex);
complex Neg_Complex(complex);

#endif
